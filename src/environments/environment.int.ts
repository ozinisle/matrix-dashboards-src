// file contents should get applied when running through the following command
// ng build --configuration=integration

export const environment = {
  production: true,
  appUrl: 'http://localhost:4200/matrix/',
  baseHref: '/matrix/'
};
